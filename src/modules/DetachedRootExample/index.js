import React from 'react'

import { Example } from '../../components/Example/index'

export const RoutePath = '/detached-roots'

export const DetachedRootExample = () => (
  <Example path={RoutePath} name="Detached Roots">
    <h1>Detached roots</h1>
  </Example>
)
